# Forcer LF pour tous les fichiers texte
* text=auto eol=lf

# Renderer snapshots preserve production HTML byte-for-byte, including spaces.
tests/Site/Service/Rendering/QuickMode/__snapshots__/*.html whitespace=-trailing-space
